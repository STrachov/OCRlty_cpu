services:
  orchestrator:
    image: ghcr.io/<OWNER>/ocrlty-orchestrator:latest
    restart: unless-stopped
    env_file: [.env]
    ports: ["8080:8080"]
    volumes:
      - ./data:/data
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
